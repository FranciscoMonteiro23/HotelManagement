@page
@model HotelManagement.Areas.Identity.Pages.Account.Manage.IndexModel
@{
    ViewData["Title"] = "Perfil";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-body">
                    <h2 class="card-title">👤 @ViewData["Title"]</h2>
                    <hr />

                    @if (!string.IsNullOrEmpty(Model.StatusMessage))
                    {
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            @Model.StatusMessage
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }

                    <div class="mb-3">
                        <label class="form-label fw-bold">Email / Nome de utilizador:</label>
                        <p class="form-control-plaintext">@Model.Username</p>
                    </div>

                    <div class="alert alert-info">
                        <strong>ℹ️ Informação:</strong> Esta é a sua página de perfil. Em desenvolvimento futuro, poderá
                        alterar a sua password e outras informações aqui.
                    </div>

                    <div class="mt-4">
                        <a asp-page="/Index" asp-area="" class="btn btn-primary">🏠 Voltar ao Início</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>